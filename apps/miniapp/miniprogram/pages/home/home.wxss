.home-page {
  padding-top: 96rpx;
}

.greeting {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 40rpx;
  padding: 0 8rpx;
}

.hello,
.hint {
  display: block;
}

.hello {
  color: #2d2520;
  font-size: 40rpx;
  font-weight: 600;
  line-height: 1.4;
}

.hint {
  margin-top: 12rpx;
  color: #8c7f76;
  font-size: 26rpx;
  line-height: 1.5;
}

.mascot {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 16rpx 48rpx rgba(183, 137, 92, 0.12);
}

.mascot .tm-icon-soup {
  color: #b7895c;
}

.hero {
  position: relative;
  overflow: hidden;
  min-height: 360rpx;
  margin-top: 48rpx;
  padding: 40rpx;
  border-radius: 48rpx;
  color: #fff;
}

.hero-bg {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 78% 20%, rgba(255, 255, 255, 0.36), transparent 180rpx),
    radial-gradient(circle at 88% 86%, rgba(245, 234, 226, 0.62), transparent 220rpx),
    linear-gradient(135deg, #b7895c 0%, #cba174 48%, #f0d8c3 100%);
  background-size: cover;
  background-position: center;
}

.hero-content {
  position: relative;
  z-index: 3;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.hero-title {
  font-size: 48rpx;
  font-weight: 700;
  text-shadow: 0 4rpx 16rpx rgba(45, 37, 32, 0.2);
}

.hero-subtitle {
  margin-top: 16rpx;
  color: rgba(255, 255, 255, 0.9);
  font-size: 26rpx;
  text-shadow: 0 4rpx 16rpx rgba(45, 37, 32, 0.2);
}

.hero-button {
  position: relative;
  z-index: 4;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 72rpx;
  margin-top: 40rpx;
  padding: 0 32rpx;
  border-radius: 36rpx;
  background: rgba(255, 255, 255, 0.96);
  color: #2d2520;
  font-size: 26rpx;
  font-weight: 600;
  box-shadow: 0 8rpx 32rpx rgba(45, 37, 32, 0.08);
}

.hero-button .icon-inline,
.section-action .icon-inline {
  margin: 0 0 0 8rpx;
}

.dish-bubbles {
  position: absolute;
  right: 32rpx;
  bottom: 32rpx;
  z-index: 2;
  display: flex;
}

.dish-bubble {
  width: 104rpx;
  height: 104rpx;
  margin-left: -20rpx;
  border: 6rpx solid rgba(255, 255, 255, 0.9);
  border-radius: 50%;
  box-shadow: 0 16rpx 48rpx rgba(45, 37, 32, 0.12);
}

.dish-bubble-placeholder {
  background:
    radial-gradient(circle at 34% 30%, rgba(255, 255, 255, 0.92), transparent 26rpx),
    linear-gradient(135deg, #f5eae2 0%, #b7895c 100%);
}

.quick-grid,
.add-actions {
  display: flex;
  gap: 24rpx;
}

.quick-grid {
  margin-top: 32rpx;
}

.quick-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex: 1 1 0;
  width: 0;
  min-width: 0;
  min-height: 160rpx;
  padding: 28rpx;
  text-align: left;
}

.quick-card > view {
  min-width: 0;
}

.quick-title,
.quick-subtitle {
  display: block;
}

.quick-title {
  color: #2d2520;
  font-size: 32rpx;
  font-weight: 600;
  white-space: nowrap;
}

.quick-subtitle {
  margin-top: 12rpx;
  color: #c5beb8;
  font-size: 24rpx;
}

.quick-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  flex: 0 0 64rpx;
  margin-left: 16rpx;
  border-radius: 20rpx;
  background: #f5eae2;
  color: #b7895c;
}

.quick-icon .tm-icon {
  color: #b7895c;
}

.add-actions {
  margin-top: 24rpx;
}

.add-action {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1 1 0;
  gap: 12rpx;
  width: 0;
  min-width: 0;
  height: 80rpx;
  border: 1rpx solid rgba(239, 231, 225, 0.6);
  border-radius: 24rpx;
  background: #f5eae2;
  color: #b7895c;
  font-size: 26rpx;
  font-weight: 600;
  box-shadow: 0 8rpx 32rpx rgba(45, 37, 32, 0.03);
}

.add-action.primary {
  color: #b7895c;
}

.section-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 48rpx 0 24rpx;
  padding: 0 8rpx;
}

.section-action {
  display: flex;
  align-items: center;
  color: #b7895c;
  font-size: 26rpx;
}

.inspiration-scroll {
  width: 100%;
  overflow: visible;
}

.dish-row {
  display: flex;
  gap: 24rpx;
  padding-bottom: 24rpx;
}

.dish-row::after {
  display: block;
  flex: 0 0 32rpx;
  content: "";
}

.dish-card {
  flex: 0 0 200rpx;
  overflow: hidden;
  padding-bottom: 24rpx;
}

.dish-cover {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 200rpx;
  height: 160rpx;
  border-radius: 32rpx 32rpx 24rpx 24rpx;
}

.dish-cover-placeholder {
  background:
    radial-gradient(circle at 18% 18%, rgba(255, 255, 255, 0.9), transparent 62rpx),
    linear-gradient(135deg, #fff8f5 0%, #f5eae2 100%);
}

.dish-name,
.dish-meta {
  display: block;
  padding: 0 20rpx;
}

.dish-name {
  margin-top: 16rpx;
  color: #2d2520;
  font-size: 28rpx;
  font-weight: 600;
}

.dish-meta {
  margin-top: 10rpx;
  color: #8c7f76;
  font-size: 22rpx;
}
