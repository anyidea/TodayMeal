.list-page {
  padding-top: 104rpx;
  padding-bottom: 180rpx;
}

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 32rpx;
  padding-right: 190rpx;
}

.page-title {
  color: #2d2520;
  font-size: 48rpx;
  font-weight: 700;
}

.tools {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.tool-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(45, 37, 32, 0.04);
  transition: all 0.2s ease;
}

.tool-btn:active {
  transform: scale(0.95);
  background: #f9f6f4;
}

.tabs {
  width: calc(100% + 64rpx);
  margin: 40rpx -32rpx 0;
  padding-left: 32rpx;
  white-space: nowrap;
}

.tab-row {
  display: inline-flex;
  gap: 16rpx;
  padding-right: 32rpx;
  white-space: nowrap;
}

.tab {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  min-width: 80rpx;
  height: 64rpx;
  padding: 0 32rpx;
  border-radius: 32rpx;
  background: #ffffff;
  color: #8c7f76;
  font-size: 26rpx;
  font-weight: 500;
  white-space: nowrap;
  box-shadow: 0 4rpx 16rpx rgba(45, 37, 32, 0.02);
  transition: all 0.3s ease;
}

.tab.active {
  background: #b7895c;
  color: #fff;
  font-weight: 600;
  box-shadow: 0 8rpx 24rpx rgba(183, 137, 92, 0.24);
}

.recipe-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  margin-top: 32rpx;
}

.recipe-item {
  display: flex;
  min-height: 200rpx;
  padding: 24rpx;
  gap: 24rpx;
}

.recipe-img {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 160rpx;
  width: 160rpx;
  height: 160rpx;
  border-radius: 24rpx;
}

.recipe-img-placeholder {
  background:
    radial-gradient(circle at 22% 18%, rgba(255, 255, 255, 0.86), transparent 56rpx),
    linear-gradient(135deg, #fff8f5 0%, #f5eae2 100%);
}

.recipe-info {
  display: flex;
  flex: 1;
  flex-direction: column;
  justify-content: space-between;
  min-width: 0;
  padding: 8rpx 0;
}

.recipe-top,
.recipe-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.recipe-name {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  color: #2d2520;
  font-size: 32rpx;
  font-weight: 700;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.more-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48rpx;
  height: 48rpx;
  flex: 0 0 48rpx;
  border-radius: 50%;
}

.more-btn:active {
  background: #fff8f5;
}

.recipe-meta {
  display: block;
  overflow: hidden;
  margin-top: 8rpx;
  color: #8c7f76;
  font-size: 24rpx;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.recipe-bottom {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.meta-tag {
  display: flex;
  align-items: center;
  min-height: 44rpx;
  padding: 0 16rpx;
  border-radius: 999rpx;
  background: #fff8f5;
  color: #8c7f76;
  font-size: 22rpx;
  font-weight: 600;
}

.favorite-tag {
  background: rgba(255, 107, 107, 0.1);
  color: #d85d5d;
}

.meta-tag .icon-inline {
  margin-right: 8rpx;
}
