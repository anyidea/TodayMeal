@import "./styles/tokens.wxss";

.page {
  min-height: 100vh;
  padding: 32rpx 32rpx calc(190rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

.card {
  background: var(--color-card);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-card);
}
